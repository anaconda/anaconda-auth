def configure_conda_for_environments(org_name: str) -> None:
    """Configure conda when environments feature is enabled."""
    # TODO: we should be using the org_id and should make this change
    # TODO: determine which conda commands to run
    pass
